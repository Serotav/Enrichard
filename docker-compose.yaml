services:
  enrichard-app:
    build:
      context: .
      dockerfile: Dockerfile
    
    container_name: enrichard_container

    ports:
      - "8501:8501"

    volumes:
      - ./app:/app
      - ./data:/app/data
    
    command: ["/start.sh"]
